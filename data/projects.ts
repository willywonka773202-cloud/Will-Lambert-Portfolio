// The portfolio is now generated live from GitHub. See lib/portfolio.ts for the
// builder, data/overrides.ts to curate a specific project, and data/config.ts
// for inclusion rules. This file just re-exports the shared types so existing
// component imports keep working.
export type { Project, ProjectStatus, ProjectDetail } from "./types";
